import { ROL_ACCESS } from "./rols.helpers";

/**
 * All routes names in the system
 */
export const ROUTERS_LIST = {
  AUTH: {
    ROOT: "auth",
    LOGIN: "login",
    REGISTER: "register",
    LOGOUT: "logout",
  },
};
